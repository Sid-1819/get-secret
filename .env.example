DATABASE_URL=""
REDIS_URL=redis://localhost:6379

# Optional. Base URL for note links in API responses. If unset: production -> https://getsecret.visionly.dev, local -> http://localhost:8080.
# PUBLIC_APP_URL=https://your-frontend.com
# PUBLIC_APP_URL=http://localhost:5173

# Required. 32-byte key: hex (64 chars) or base64 (44 chars). Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
