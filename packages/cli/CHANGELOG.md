# @getsecret/cli

## 2.0.0

### Major Changes

- Initial release as `@getsecret/cli` in the getsecret monorepo.
- Commands: `create`, `get`.
- Config via CLI flags, `GETSECRET_API_URL` / `GETSECRET_WEB_URL`, or `~/.config/getsecret/config.json`.
